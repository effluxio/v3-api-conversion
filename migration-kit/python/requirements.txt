# Efflux v3 Python client — no required third-party dependencies.
# The client uses only Python standard library (urllib, json, dataclasses).
#
# Optional — install these for WebSocket stream support:
# websockets>=12.0
#
# Optional — for async support:
# aiohttp>=3.9.0
# websockets>=12.0
